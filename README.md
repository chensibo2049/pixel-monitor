# 像素课代表

面向大学生的垂直 AI 生图站。支持 image-2 与 image-2-Pro 两档按次计费、生成历史、校园代理申请、独立分站、订单分佣和后台价格配置。

## 运行环境

运行时需要 D1 数据库、R2 图片存储，以及以下服务端环境变量：

- `IMAGE_API_KEY`：上游生图服务密钥
- `IMAGE_API_BASE_URL`：OpenAI 兼容接口地址（填写到 `/v1`）
- `ADMIN_EMAILS`：后台管理员邮箱，多个邮箱用英文逗号分隔

密钥仅在服务端调用，不会进入浏览器代码。
